<h2>pet _form partial</h2>

<%= form_for owner_pets_path(@owner, @pet) do |f| %>

  <div class="form-group">
    <%= f.text_field :name, class: "form-control", placeholder: "Pet name", autofocus: true %>
  </div>
  <div class="form-group">
    <%= f.text_field :breed, class: "form-control", placeholder: "Pet breed" %>
  </div>
  <div class="form-group">
    <%= f.submit "Save pet", class: "btn btn-block btn-success" %>
  </div>
<% end %>
<hr>
<%= link_to "Back to owner's pets", owner_pets_path %>

<!-- form_for @pet do |f| -->
<!-- form_for :pet, url: owner_pets_path, method: "post" do |f| -->
